ReferenceRange:
  title: ReferenceRange
  type: object
  description: A reference range, set of ordered low and high values by a clinician.
  properties:
    type:
      type: string
      description: Type of reference range
      enum:
        - NORMAL
        - CRITICAL
    text:
      type: string
      description: Text based reference range.
    low:
      type: number
      description: Low range, if relevant.
    high:
      type: number
      description: High range, if relevant.
