description: User Details
content:
  application/json:
    schema:
      allOf:
        - $ref: '../schemas/User.yaml'
        - type: object
          properties:
            organization:
              $ref: '../schemas/Organization.yaml'

