description: Create a new practice request body
required: true
content:
  application/json:
    schema:
      type: object
      properties:
        name:
          type: string
          description: The name of the practice
          example: "My Practice"
      required:
        - name
