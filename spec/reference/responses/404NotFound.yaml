description: |
  **Not Found**
  
  The server cannot find the requested resource.
content:
  application/json:
    schema:
      $ref: '../types/Error.yaml'
    example:
      statusCode: 404
      error: Not Found
      message: The requested resource was not found.
