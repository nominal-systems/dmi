content:
  application/json:
    schema:
      type: object
      properties:
        password:
          type: string
          format: password
          example: NewPassword
      required:
        - password
    examples:
      Update User password:
        value:
          password: NewPassword
